# ReMeal — Surplus Food Marketplace & Delivery Platform

> "Where Surplus Food Finds Its Next Meal."

## Day 2 Setup — What's Done

- ✅ `frontend/` — React + Vite + Tailwind CSS v4 + Axios + React Router
- ✅ `backend/` — Node.js + Express + Mongoose (MongoDB) + dotenv + cors + nodemon

## How to Run

### 1. Backend
```bash
cd backend
npm install
# Copy .env.example to .env and fill in your real MongoDB Atlas URI
npm run dev
```
Server runs at: http://localhost:5000

### 2. Frontend
```bash
cd frontend
npm install
npm run dev
```
App runs at: http://localhost:5173

## Folder Structure

```
remeal/
├── frontend/     -> React app (Vite)
└── backend/      -> Express API
```

## Tech Stack
React, Vite, Tailwind CSS v4, Axios, React Router, Node.js, Express, MongoDB Atlas, Mongoose, JWT, bcrypt (added Day 4)
